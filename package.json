{
  "name": "mongoid",
  "version": "1.0.0",
  "description": "Simple MongoDB ORM",
  "author": "Joost Lubach",
  "license": "MIT",
  "scripts": {
    "build": "(pnpm build:prepare && pnpm build:tsc && pnpm build:tsc-alias && pnpm build:copy && pnpm build:rsync); pnpm build:cleanup",
    "build:prepare": "test -x .build && rm -rf .build; mkdir -p .build",
    "build:tsc": "tsc -p tsconfig.cjs.json --outDir .build/cjs && tsc -p tsconfig.esm.json --outDir .build/esm",
    "build:tsc-alias": "tsc-alias -p tsconfig.cjs.json --outDir .build/cjs && tsc-alias -p tsconfig.esm.json --outDir .build/esm",
    "build:copy": "copyfiles -u 1 src/**/*.yml src/**/*.css .build/cjs && copyfiles -u 1 src/**/*.yml src/**/*.css .build/esm",
    "build:rsync": "rsync -uvaz --delete .build/ dist/",
    "build:cleanup": "rm -rf .build",
    "clean": "rimraf dist",
    "test": "NODE_ENV=test jest"
  },
  "exports": {
    ".": {
      "require": {
        "types": "./dist/cjs/frontend/index.d.ts",
        "default": "./dist/cjs/frontend/index.js"
      },
      "import": {
        "types": "./dist/esm/frontend/index.d.ts",
        "default": "./dist/esm/frontend/index.js"
      }
    },
    "./backend": {
      "require": {
        "types": "./dist/cjs/backend/index.d.ts",
        "default": "./dist/cjs/backend/index.js"
      },
      "import": {
        "types": "./dist/esm/backend/index.d.ts",
        "default": "./dist/esm/backend/index.js"
      }
    },
    "./types": {
      "require": "./dist/cjs/backend/types/index.js",
      "import": "./dist/esm/backend/types/index.js",
      "types": "./dist/cjs/backend/types/index.d.ts"
    }
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "inflected": "^2.0.4",
    "mongodb": "^6.0.0",
    "strip-ansi": "^5.2.0",
    "validator": "workspace:^",
    "winston": "^3.10.0",
    "ytil": "workspace:^"
  },
  "devDependencies": {
    "@types/inflected": "^1.1.29",
    "copyfiles": "^2.4.1",
    "jest": "^29.3.1",
    "rimraf": "^5.0.1",
    "ts-jest": "^29.0.3",
    "tsc-alias": "^1.8.7",
    "typescript": "^5.1.6"
  }
}
